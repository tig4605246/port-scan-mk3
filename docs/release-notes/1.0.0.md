# 1.0.0 Release Notes

## New features
- Bootstrap CLI with `scan` and `validate` commands.
- Add CIDR/port parsing with overlap fail-fast validation.
- Add per-CIDR leaky bucket and pause gate primitives.
- Add resume state persistence and integration test harness.

## Bug fixes
- N/A (initial release).

## Breaking changes
- Initial release, no migration required.
